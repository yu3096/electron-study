{
  "name": "electron_chat_app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "babel --watch --out-dir js src",
    "build": "babel --out-dir js src",
    "start": "babel --out-dir js src && electron ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-react": "^6.16.0",
    "electron": "^19.0.0",
    "electron-devtools-installer": "^3.2.0"
  },
  "dependencies": {
    "firebase": "^9.9.1",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-router": "^3.0.0"
  }
}
